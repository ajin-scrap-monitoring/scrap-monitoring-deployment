#!/usr/bin/env bash
set -euo pipefail

if [[ "${1:-}" == "--help" ]]; then
  printf '%s\n' 'Usage: ensure-server-certificate'
  exit 0
fi

printf '%s\n' 'ensure-server-certificate is not implemented.' >&2
exit 1
