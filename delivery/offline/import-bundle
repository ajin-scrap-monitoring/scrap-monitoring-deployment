#!/usr/bin/env bash
set -euo pipefail

if [[ "${1:-}" == "--help" ]]; then
  printf '%s\n' 'Usage: import-bundle <bundle> <checksum-manifest>'
  exit 0
fi

printf '%s\n' 'import-bundle is not implemented.' >&2
exit 1
