#!/usr/bin/env bash
set -euo pipefail

if [[ "${1:-}" == "--help" ]]; then
  printf '%s\n' 'Usage: build-bundle <target> <image-archive> <output-directory>'
  exit 0
fi

printf '%s\n' 'build-bundle is not implemented.' >&2
exit 1
